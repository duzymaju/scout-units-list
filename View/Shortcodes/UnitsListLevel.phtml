<?php if (count($this->elements) > 0): ?>
    <ul>
        <?php foreach ($this->elements as $element): ?>
            <li>
                <span><?php echo $this->escape($element['current']->getName()); ?></span>
                <?php echo $element['dependents']; ?>
            </li>
        <?php endforeach; ?>
    </ul>
<?php endif; ?>
